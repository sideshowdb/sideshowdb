---
title: Getting Started
order: 1
---

# Getting Started

Install Zig 0.16, build the CLI with `zig build`, and explore the browser client with `zig build wasm`.

The shortest path is:

1. Clone the repo.
2. Build the CLI.
3. Run the browser client against a sample repository.
